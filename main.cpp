#include "read_write.h"
#include "basic_change.h"
int main() {
	char word[10];
	char cword[10]="as";
	input(word);
	code(word, cword);
	output(cword);
	 //char word[] = GetWord();
	// char cword[] = Code(word);
	  //DeCode(cword);
		return 0;
}